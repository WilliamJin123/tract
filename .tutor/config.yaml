# Generated by /tutor:init — edit freely
project:
  summary: "Git-like version control for LLM context windows"

audience:
  profile: "Solo developer building and maintaining Trace; writing for own future reference and deep understanding"
  technical_level: intermediate  # beginner | intermediate | expert
  assumed_knowledge:
    - Python fundamentals (classes, decorators, context managers, typing, dataclasses)

focus:
  conceptual: heavy
  design_choices: heavy
  implementation: heavy
  connections: heavy
  examples: heavy

triggers:
  mode: both  # manual | pre | post | both

output:
  directory: tutorials/
  style: narrative  # narrative | reference | faq
  naming: "{nn}-{slug}.md"
  frontmatter: true
  frontmatter_fields:
    - date
    - summary
    - audience

presets:
  quick:
    focus_override:
      conceptual: light
      implementation: skip
    length: brief
  deep-dive:
    focus_override:
      conceptual: heavy
      design_choices: heavy
      implementation: heavy
      connections: heavy
      examples: heavy
    length: comprehensive
