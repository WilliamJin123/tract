"""Retry and Validation

Validate LLM output and retry with steering when it fails. The retry
protocol is operation-agnostic — it works for chat, compression, merge
resolution, or any callable.

Demonstrates: chat(validator=), retry_with_steering, RetryResult,
              RetryExhaustedError, purify=True, provenance_note=True
"""


def main():
    pass


if __name__ == "__main__":
    main()
