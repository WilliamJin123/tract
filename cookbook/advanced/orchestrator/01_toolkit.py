"""
Toolkit — as_tools() exposes tract operations as LLM tool schemas. Three profiles:
self (full CRUD), supervisor (read + high-level), observer (read-only). ToolExecutor
for executing tool calls against a tract.
"""


def main(): pass


if __name__ == "__main__":
    main()
