"""
Agent Serialization — to_dict() for JSON state snapshots, to_tools() for LLM tool schemas,
describe_api() for natural language descriptions, apply_decision() and execute_tool() for
agent dispatch, _public_actions whitelist for safety.
"""


def main(): pass


if __name__ == "__main__":
    main()
