"""
Built-In Policies — CompressPolicy (auto-compress at budget threshold), PinPolicy
(auto-pin system prompts), BranchPolicy (detect topic drift), ArchivePolicy (archive
stale branches). Configure with configure_policies().
"""


def main(): pass


if __name__ == "__main__":
    main()
